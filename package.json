{
  "name": "cypressproject1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cy:open":"cypress open",
    "cy:run": "cypress run",
    "cy:run:chrome": "cyress run--browser chrome",
    "test": "echo \"Error: no test specified\" && exit 1",
    "merge:reports": "npx mochawesome-merge cypress/reports/*.json >cypress/reports/mocha/merged.json",
    "generate:html": "npx marge cypress/reports/mocha/merged.json --reportDir cypress/reports/mocha",
    "report:full": "npm run merge:reports && npm run generate:html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^15.3.0"
  },
  "description": "",
  "devDependencies": {
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^4.4.1",
    "mochawesome-report-generator": "^6.3.0"
  }
}
